"""One-shot Typer commands.

stdout carries JSON and nothing else; logs and progress go to stderr. The commands land
with phase 7; until then ``python -m numenews.cli`` runs the placeholder in ``__main__.py``.
"""
