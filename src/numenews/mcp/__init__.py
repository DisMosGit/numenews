"""MCP server and its tools (``FastMCP``).

Each tool returns a Pydantic model. The server and its nine tools land with phase 6;
until then ``python -m numenews.mcp`` runs the placeholder in ``__main__.py``.
"""
