"""Pure numerology: digit reduction, master numbers and gematria.

This package is deliberately dependency-free: it must never import from ``news``,
``vector``, ``agents`` or ``mcp``, and it performs no I/O. The functions land with phase 1.
"""
