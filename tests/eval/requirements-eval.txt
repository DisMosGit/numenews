# The ragas evaluation environment (`.venv-eval`), installed by `make eval-env`.
#
# ragas is *not* a dependency group in `pyproject.toml`, and these pins are deliberately outside
# `uv.lock`: ragas and `pydantic-ai-slim[openai]` cannot be resolved together (every `instructor`
# release pins `jiter<0.15`, while `openai>=3.8` — which `pydantic-ai-slim` requires — needs
# `jiter>=0.16`). `make eval-env` therefore syncs the locked environment with
# `--no-install-package pydantic-ai-slim` first and adds ragas here. See ADR 0013.
#
# The second pin is an upstream bug: ragas 0.4.3 imports
# `langchain_community.chat_models.vertexai`, which langchain-community removed in 0.4.
ragas==0.4.3
langchain-community<0.4
